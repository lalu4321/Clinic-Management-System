// Header merged into Navbar.jsx — this component is kept for backward compatibility
// but renders nothing. The Navbar now handles the full top-bar including the logo.
export default function Header() {
  return null;
}
